import { Button, Typography } from "@mui/material";

const LoginPage = () => {
  return (
    <>
      <Typography variant="h1">Vocabi</Typography>
      <Button>Login</Button>
    </>
  );
};

export default LoginPage;
